pipeline {
    agent any
 tools {
        maven 'maven'
    }

    stages {
        stage('eureka-server') {
		dir('eureka-server') {
            steps {
                 sh 'mvn clean package -Dmaven.test.skip=true'

                }
            }
	}    
        }
		
    }
