pipeline {
    agent any
 tools {
        maven 'maven'
    }
    stages {
        stage('config-server') {
            steps {
		    dir('config-server') {
                 sh 'mvn clean package -Dmaven.test.skip=true'

                }
            }
	}    
        }
		
    }
